<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('group_project', function (Blueprint $table) {
            $table->id();
            $table->foreignUuid('project_group_id')->constrained()->cascadeOnDelete();
            $table->foreignUuid('project_id')->constrained()->cascadeOnDelete();
            $table->timestamps();

            // Prevent the same project from being added to the same group twice
            $table->unique(['project_group_id', 'project_id']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('group_project');
    }
};
